export enum FieldType {
  Text = "text",
  Textarea = "textarea",
  Number = "number",
  Boolean = "boolean",
  Date = "date",
  Datetime = "datetime",
  Select = "select",
  MultiSelect = "multi_select",
  Relation = "relation",
  Json = "json",
}
